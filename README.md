# sails-code-navigator README

The extension allows to go to definition of Sails Services and Models.

The config contains an array of paths to your modules, the defaults are `./api/services` and `./api/models`
